import Layout from 'components/Layout';
import { FC } from 'react';

const Home: FC = () => {
  return (
    <div className="h-screen">
      <Layout>Layout</Layout>
    </div>
  );
};

export default Home;
